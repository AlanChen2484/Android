<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.easr.finalexam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/foundheart"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.RegisterActivity"
            android:theme="@style/Translucent"/>

        <activity android:name=".home.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".home.SpecificActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".find.FoundActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".email.EmailActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".person.SpiActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".other.AddPrinter"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".other.SelectPrinter"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".person.RetroactionActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".person.PersonalInformationActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".person.SettingActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".person.AccountsActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".email.ChatSxActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".find.MaterialShowActivity"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>